# TODO

- [ ] Add unit tests for the `TelonHost` handshake mechanism.
- [ ] Add unit tests for the `TelonClient` origin validation logic.
- [ ] Document an example of a "Banner UI" for the Auth Bridge popup in the README.
- [ ] Investigate fallback mechanisms using `BroadcastChannel` for scenarios where `postMessage` to `window.opener` is restricted by specific browser flags.
- [ ] Automate bundle size checks in CI to ensure the library remains lightweight.
